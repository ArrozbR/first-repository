#include <bits/stdc++.h>

using namespace std;

int main(){

    int n, f;
    string aux;
    while(cin >> n && n != 0){
        vector<pair<string, int>> v;
        for(int i  = 0; i < n; i++){
            cin >> aux >> f;
            v.push_back(make_pair(aux, f));
        }
        while(v.size() > 1){
            
        }
    }

    return 0;
}